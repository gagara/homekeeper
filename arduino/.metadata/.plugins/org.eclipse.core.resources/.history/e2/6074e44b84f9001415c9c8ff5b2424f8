// Do not remove the include below
#include "Bt.h"

void setup() {
	Serial.begin(9600);
}

void loop() {
	if (Serial.available() > 0) {
		Serial.println("[" + Serial.readStringUntil('\n') + "]");
	}
}
